name: hello world
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4 

      - name: Welcome message
        run: echo "Hello World"

      - name: list files
        run: ls -la

      - name: Read file
        run: cat README.md
